---
phase: 04-signature-standardization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/audit_signatures.py
  - src/pytest_generator/assert_utilities.py
autonomous: true
requirements:
  - REQ-CONS-01
  - REQ-CONS-02

must_haves:
  truths:
    - "Running python scripts/audit_signatures.py exits with code 0 and reports zero violations"
    - "All check_structure_X and check_answer_X functions have -> CheckResult annotation"
    - "check_structure_dendrogram uses student_answer as its first parameter"
    - "check_answer_dendrogram uses student_answer and instructor_answer as its first two parameters"
    - "All Phase 1 regression tests pass after the changes"
  artifacts:
    - path: "scripts/audit_signatures.py"
      provides: "AST-based audit that enumerates module-level check_structure_* and check_answer_* functions and reports annotation or naming violations"
      exports: []
    - path: "src/pytest_generator/assert_utilities.py"
      provides: "Fully annotated and consistently named checker functions"
      contains: "-> CheckResult"
  key_links:
    - from: "scripts/audit_signatures.py"
      to: "src/pytest_generator/assert_utilities.py"
      via: "ast.parse of assert_utilities.py"
      pattern: "ast.parse"
    - from: "check_answer_dendrogram body"
      to: "student_answer / instructor_answer params"
      via: "body variable references match renamed params"
      pattern: "student_answer|instructor_answer"
---

<objective>
Apply all signature standardization changes to assert_utilities.py, gated by an audit script written first.

Purpose: REQ-CONS-01 requires consistent parameter naming (dendrogram pair); REQ-CONS-02 requires return annotations on all 4 scatterplot functions and uniform use of the CheckResult alias across all 91 public checker functions. The audit script provides the objective verification artifact for the success criterion "running an audit script confirms zero violations."

Output: scripts/audit_signatures.py (reusable audit tool), assert_utilities.py with all 4 missing annotations added, all bare tuple[bool, str] annotations replaced with CheckResult, and dendrogram parameter names corrected.
</objective>

<execution_context>
@/Users/erlebach/.claude/get-shit-done/workflows/execute-plan.md
@/Users/erlebach/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-signature-standardization/04-RESEARCH.md
@.planning/phases/04-signature-standardization/04-VALIDATION.md

<interfaces>
<!-- Key facts about assert_utilities.py extracted from AST audit (in RESEARCH.md) -->

Public checker functions follow exactly two prefixes:
  check_structure_*   (standard sig: check_structure_X(student_answer, ...) -> CheckResult)
  check_answer_*      (standard sig: check_answer_X(student_answer, instructor_answer, ...) -> CheckResult)

CheckResult alias already defined at module scope in SECTION 1:
  CheckResult: TypeAlias = tuple[bool, str]

Violations to fix:

  REQ-CONS-02 — missing return annotations (add -> CheckResult):
    L2447: check_structure_scatterplot2d(student_answer)
    L2478: check_structure_scatterplot3d(student_answer)
    L4644: check_answer_scatterplot2d(student_answer, instructor_answer, options, validation_functions)
    L4726: check_answer_scatterplot3d(student_answer, instructor_answer, options, validation_functions)

  REQ-CONS-01 — parameter naming deviations (rename param + all body references):
    check_structure_dendrogram: student_dendro -> student_answer
    check_answer_dendrogram:    student_dendro -> student_answer
                                instructor_dendro -> instructor_answer

Private helpers to NOT touch (different return types, intentional):
  check_float, check_int, check_list_float, check_str, check_dict_str_str, check_key_structure,
  and 6 others. These do NOT use check_structure_ or check_answer_ prefixes.

Nested function to NOT touch:
  check_grid_status (nested inside check_answer_scatterplot2d — not module-level)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/audit_signatures.py</name>
  <files>scripts/audit_signatures.py</files>
  <action>
Create scripts/audit_signatures.py. The script must:

1. Open src/pytest_generator/assert_utilities.py and parse it with ast.parse().
2. Walk only module-level FunctionDef nodes (tree.body, not ast.walk to avoid nested functions).
3. Classify each node: include only names starting with "check_structure_" or "check_answer_". Skip everything else.
4. For each included function, check:
   a. Return annotation: ast.unparse(node.returns) if node.returns else None. Flag if not "CheckResult" (after Phase 4 all must use alias, not bare tuple[bool, str]).
   b. Parameter naming: first param must be "student_answer". For check_answer_* functions, second param must be "instructor_answer". Read param names from node.args.args[*].arg.
5. Collect all violations into a list. Print each as: "L{lineno}: {name} — {reason}".
6. Exit with sys.exit(1) if any violations; sys.exit(0) if zero.

Important: Only walk tree.body (top-level statements). Do not use ast.walk() on the full tree — that would descend into nested functions like check_grid_status.

Example output when violations exist:
  L2447: check_structure_scatterplot2d — missing return annotation
  L2792: check_answer_dendrogram — param[0] is 'student_dendro', expected 'student_answer'

Example output when clean:
  OK: 91 check_structure_* / check_answer_* functions audited, 0 violations.
  </action>
  <verify>
    <automated>cd /Users/erlebach/src/2026/pytest_generator && python scripts/audit_signatures.py; echo "exit: $?"</automated>
  </verify>
  <done>Script runs without Python errors. Before fixes are applied it should report violations (non-zero exit). Script output lists the 4 missing-annotation functions and the 2 dendrogram param deviations.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix missing annotations and apply CheckResult alias (REQ-CONS-02)</name>
  <files>src/pytest_generator/assert_utilities.py</files>
  <action>
Make two targeted changes to assert_utilities.py:

CHANGE A — Add missing return annotations to 4 scatterplot functions.
Find these exact def lines (line numbers are approximate; confirm by searching for the def):
  def check_structure_scatterplot2d(student_answer):
  def check_structure_scatterplot3d(student_answer):
  def check_answer_scatterplot2d(student_answer, instructor_answer, options, validation_functions):
  def check_answer_scatterplot3d(student_answer, instructor_answer, options, validation_functions):

For each, add " -> CheckResult" before the colon at end of the def line. Result:
  def check_structure_scatterplot2d(student_answer) -> CheckResult:
  def check_structure_scatterplot3d(student_answer) -> CheckResult:
  def check_answer_scatterplot2d(student_answer, instructor_answer, options, validation_functions) -> CheckResult:
  def check_answer_scatterplot3d(student_answer, instructor_answer, options, validation_functions) -> CheckResult:

CHANGE B — Replace all bare "tuple[bool, str]" return annotations with "CheckResult" for public checker functions.
Scope: only def lines for check_structure_* and check_answer_* functions. Do NOT change:
  - Private helpers (check_float, check_int, etc.)
  - check_key_structure (returns bool, not tuple)
  - Any non-def lines (e.g., comments, docstrings, variable annotations)

Safe approach: read the file, process each line. If a line matches the pattern:
  ^def (check_structure_|check_answer_)\w+\(.*\) -> tuple\[bool, str\]:$
replace "-> tuple[bool, str]:" with "-> CheckResult:"

Write the result back. This is a mechanical text transformation — use a Python one-liner or small script to do it, then inspect a diff to confirm only the intended lines changed.

Sanity check: after the change, grep for "-> tuple\[bool, str\]" in assert_utilities.py. Only private helpers and check_key_structure should remain (those do not start with check_structure_ or check_answer_).
  </action>
  <verify>
    <automated>cd /Users/erlebach/src/2026/pytest_generator && uv run pytest tests/test_assert_utilities.py -x -q</automated>
  </verify>
  <done>pytest passes. grep for "-> tuple[bool, str]" in assert_utilities.py shows no check_structure_* or check_answer_* functions. The 4 scatterplot functions now have -> CheckResult.</done>
</task>

<task type="auto">
  <name>Task 3: Fix dendrogram parameter naming (REQ-CONS-01)</name>
  <files>src/pytest_generator/assert_utilities.py</files>
  <action>
Rename parameters in two functions. Both renames must update the def line AND every reference in the function body.

FUNCTION 1: check_structure_dendrogram
  Find: def check_structure_dendrogram(student_dendro, ...) -> CheckResult:
  (This function may already have -> CheckResult after Task 2; if not, add it here too.)
  Rename: student_dendro -> student_answer in the def signature AND all body lines within this function.
  Scope: only lines between this def and the next top-level def (or end of file).

FUNCTION 2: check_answer_dendrogram
  Find: def check_answer_dendrogram(student_dendro, instructor_dendro, rel_tol) -> CheckResult:
  Rename: student_dendro -> student_answer AND instructor_dendro -> instructor_answer in the def signature AND all body lines within this function.
  Scope: only lines between this def and the next top-level def.

SAFE RENAME APPROACH:
  1. Identify the line range for each function using the AST (start line = node.lineno, end line = node.end_lineno).
  2. Within that line range only, do a string replace of the old param name with the new param name.
  3. Be precise: replace "student_dendro" not "student_dendro_something". Use word-boundary-aware replacement or verify the file contains no compound names.

After renaming, verify no occurrence of "student_dendro" or "instructor_dendro" remains in assert_utilities.py:
  grep "student_dendro\|instructor_dendro" src/pytest_generator/assert_utilities.py
Should return empty output.
  </action>
  <verify>
    <automated>cd /Users/erlebach/src/2026/pytest_generator && uv run pytest tests/test_assert_utilities.py -k dendrogram -x -q && python scripts/audit_signatures.py</automated>
  </verify>
  <done>dendrogram tests pass. audit_signatures.py exits 0 with "0 violations" message. No occurrence of "student_dendro" or "instructor_dendro" remains in the file.</done>
</task>

</tasks>

<verification>
After all tasks complete, run the full regression suite and the audit script:

  cd /Users/erlebach/src/2026/pytest_generator
  uv run pytest tests/ -q
  python scripts/audit_signatures.py

Both must succeed. The audit script must report "0 violations."
</verification>

<success_criteria>
- scripts/audit_signatures.py exists and exits 0 after fixes
- All 91 module-level check_structure_* and check_answer_* functions use -> CheckResult
- check_structure_dendrogram first param is student_answer
- check_answer_dendrogram first two params are student_answer, instructor_answer
- uv run pytest tests/ -q is fully green
- No occurrence of student_dendro or instructor_dendro in assert_utilities.py
</success_criteria>

<output>
After completion, create .planning/phases/04-signature-standardization/04-01-SUMMARY.md with:
- What was done (3 tasks: audit script, annotation fixes, dendrogram rename)
- Files modified
- Verification results (pytest output, audit script output)
- Any deviations from plan
</output>
