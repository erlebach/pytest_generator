---
phase: 06-detailed-refactoring
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - src/pytest_generator/assert_utilities.py
autonomous: true
requirements: [REQ-AUDIT-01]
gap_closure: true

must_haves:
  truths:
    - "REQ-AUDIT-01 is defined in REQUIREMENTS.md with a description and traceability entry"
    - "All 10 # msg_list commented-out dead-code lines are deleted from assert_utilities.py"
    - "Lines 1986-1987 (dead commented imports) are deleted from check_structure_lineplot"
    - "Lines 1992-1996 (bare triple-quoted string block) are deleted from check_structure_lineplot"
    - "All 183 regression tests continue to pass after deletions"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQ-AUDIT-01 definition under AUDIT section and traceability row"
      contains: "REQ-AUDIT-01"
    - path: "src/pytest_generator/assert_utilities.py"
      provides: "Dead code fully removed — no # msg_list lines, no dead commented imports inside check_structure_lineplot, no bare triple-quoted block inside check_structure_lineplot"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "ROADMAP.md and 06-01-PLAN.md / 06-02-PLAN.md"
      via: "REQ-AUDIT-01 definition now matches references"
      pattern: "REQ-AUDIT-01"
    - from: "tests/test_assert_utilities.py"
      to: "src/pytest_generator/assert_utilities.py"
      via: "183 tests still pass after line deletions"
      pattern: "183 passed"
---

<objective>
Close three verification gaps from Phase 6: add the missing REQ-AUDIT-01 requirement definition, and delete the remaining dead-code lines in assert_utilities.py that were overlooked during 06-02 execution.

Purpose: Phase 6 verification scored 7/10 must-haves. These three gaps are incomplete dead-code cleanup and a documentation traceability gap — not correctness failures. Closing them completes the phase.
Output: Updated REQUIREMENTS.md with REQ-AUDIT-01 defined; assert_utilities.py with all identified dead-code clusters removed.
</objective>

<execution_context>
@/Users/erlebach/.claude/get-shit-done/workflows/execute-plan.md
@/Users/erlebach/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-detailed-refactoring/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add REQ-AUDIT-01 definition and traceability to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Add a new section "### AUDIT — Code Quality" under the Active section (after the EXTENSIBILITY section), containing:

```
### AUDIT — Code Quality

- **REQ-AUDIT-01**: Audit assert_utilities.py for code quality issues (debug dead code, logic bugs, stale comments, print statements) and fix all confirmed problems
```

Then add a row to the Traceability table at the bottom:

```
| REQ-AUDIT-01 | Phase 6 | Complete |
```

Do not alter any other content in REQUIREMENTS.md.
  </action>
  <verify>
    <automated>grep -n "REQ-AUDIT-01" /Users/erlebach/src/2026/pytest_generator/.planning/REQUIREMENTS.md</automated>
  </verify>
  <done>REQUIREMENTS.md contains REQ-AUDIT-01 definition under AUDIT section and a Complete traceability row for Phase 6.</done>
</task>

<task type="auto">
  <name>Task 2: Delete # msg_list dead-code lines from assert_utilities.py</name>
  <files>src/pytest_generator/assert_utilities.py</files>
  <action>
Delete the following line ranges from assert_utilities.py. These are all `# msg_list.append(...)` or `# msg_list +=` commented-out dead-code lines.

IMPORTANT: Delete in reverse order (highest line numbers first) to prevent line-number shifts from affecting subsequent deletions.

Deletion order (bottom to top):
1. Lines 4356-4357: commented msg_list += pair
2. Lines 3350-3352: DEBUG cluster (3 lines)
3. Lines 3167-3169: DEBUG cluster (3 lines)
4. Lines 530-531: commented msg_list.append in check_answer_bool helper (2 lines)

After each deletion, verify no adjacent code is disturbed. The surrounding lines should be logic statements, not part of the removed block.

Total: 10 lines deleted.
  </action>
  <verify>
    <automated>grep -c "# msg_list" /Users/erlebach/src/2026/pytest_generator/src/pytest_generator/assert_utilities.py || true</automated>
  </verify>
  <done>grep -c "# msg_list" returns 0. No # msg_list lines remain in the file.</done>
</task>

<task type="auto">
  <name>Task 3: Delete dead commented imports and bare triple-quoted block in check_structure_lineplot</name>
  <files>src/pytest_generator/assert_utilities.py</files>
  <action>
Delete the following from inside the `check_structure_lineplot` function body. After Task 2 deletions shift line numbers, re-identify the target lines by content before deleting.

Target 1 — Two dead commented import lines (find by content):
```
# from matplotlib.figure import Figure
# from matplotlib.lines import Line2D
```
These appear inside the function body of `check_structure_lineplot`. Delete both lines. Active imports of the same symbols exist elsewhere in the file.

Target 2 — Bare triple-quoted string block (find by content):
A bare string literal containing commented-out print statements. It starts with `"""` immediately after the two deleted import lines and spans 5 lines (1992-1996 before Task 2 shifts). Find the block by searching for a triple-quote that is NOT a docstring (i.e., not at the start of a function/class and not assigned to anything) inside `check_structure_lineplot`, containing commented print statements.

Delete the entire triple-quoted block (all 5 lines including the opening and closing `"""`).

Total: 7 lines deleted in this task.

After deletion, run regression tests:
```
cd /Users/erlebach/src/2026/pytest_generator && uv run pytest tests/test_assert_utilities.py -q
```

The output must show `183 passed`.
  </action>
  <verify>
    <automated>cd /Users/erlebach/src/2026/pytest_generator && uv run pytest tests/test_assert_utilities.py -q 2>&1 | tail -5</automated>
  </verify>
  <done>183 tests pass. The two commented import lines and the bare triple-quoted block no longer appear in check_structure_lineplot (confirmed by grep for "from matplotlib.figure import Figure" inside a comment and for the bare triple-quote pattern).</done>
</task>

</tasks>

<verification>
After all tasks:
1. `grep -n "REQ-AUDIT-01" .planning/REQUIREMENTS.md` — shows definition and traceability row
2. `grep -c "# msg_list" src/pytest_generator/assert_utilities.py` — returns 0
3. `uv run pytest tests/test_assert_utilities.py -q` — shows `183 passed`
4. No bare triple-quoted string blocks remain inside `check_structure_lineplot`
</verification>

<success_criteria>
- REQ-AUDIT-01 defined in REQUIREMENTS.md with Phase 6 Complete traceability entry
- Zero `# msg_list` lines in assert_utilities.py (was 10)
- Zero dead commented imports inside check_structure_lineplot (was 2 lines)
- Zero bare triple-quoted blocks inside check_structure_lineplot (was 5 lines)
- 183 regression tests pass (no regressions introduced)
</success_criteria>

<output>
After completion, create `.planning/phases/06-detailed-refactoring/06-03-SUMMARY.md`
</output>
