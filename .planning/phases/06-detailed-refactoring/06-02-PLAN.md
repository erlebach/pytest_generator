---
phase: 06-detailed-refactoring
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src/pytest_generator/assert_utilities.py
autonomous: true
requirements:
  - REQ-AUDIT-01

must_haves:
  truths:
    - "No live print() statements remain anywhere in assert_utilities.py"
    - "No pprint calls remain in assert_utilities.py"
    - "The pprint import line is removed (it becomes unused after pprint call removal)"
    - "All lines using the # ! commented-code convention are gone"
    - "All three TODO markers are resolved or annotated with a plain-comment rationale"
    - "All 183 Phase 1 regression tests continue to pass"
  artifacts:
    - path: "src/pytest_generator/assert_utilities.py"
      provides: "clean file — no debug prints, no pprint, no # ! dead code, no unresolved TODOs"
  key_links:
    - from: "src/pytest_generator/assert_utilities.py"
      to: "zero grep matches"
      via: "grep -c 'print(' assert_utilities.py → 0"
      pattern: "^\\s*print\\("
---

<objective>
Remove all 67 debug print() statements, 2 pprint() calls, the now-unused pprint import, all 28 commented-out code lines (# ! convention and # msg_list.append lines), and resolve the 3 active TODO markers.

Purpose: Debug prints fire on every call in production grading, polluting stdout. Commented-out dead code creates confusion about intended behavior. TODOs that have no action plan become permanent noise.
Output: assert_utilities.py with zero debug output statements, zero dead-code comment lines, and every TODO either fixed or converted to a plain explanatory comment.
</objective>

<execution_context>
@/Users/erlebach/.claude/get-shit-done/workflows/execute-plan.md
@/Users/erlebach/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-detailed-refactoring/06-RESEARCH.md
@.planning/phases/06-detailed-refactoring/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove all print() and pprint() calls, remove pprint import</name>
  <files>src/pytest_generator/assert_utilities.py</files>
  <action>
Delete every line that is solely a print() or pprint() call (i.e., the whole line is a debug statement, not a return value or argument). Key clusters from research:

- check_float ~line 115, 120 (2 lines)
- check_list_int ~line 217 (1 line)
- check_answer_bool helper ~lines 546-547 (2 lines)
- check_structure_bool ~line 684 (1 line)
- check_structure_dict_str_int ~lines 1362-1363 (2 lines)
- check_structure_dict_str_list_int ~lines 1418-1419 (2 lines)
- check_structure_list_tuple_float ~lines 2365-2387 (6 lines)
- check_structure_set_str ~lines 2572, 2604, 2609, 2614, 2619, 2623 (6 lines)
- check_answer_dict_int_dict_str_any ~lines 2869-2875 (6 lines including 2 pprint calls)
- check_answer_dict_int_float ~lines 2978, 2981 (2 lines)
- check_answer_dict_str_float ~lines 3314, 3330, 3332, 3394, 3399 (5 lines)
- check_answer_dict_str_int ~lines 3414-3415, 3438, 3442 (4 lines)
- check_answer_dict_str_list_int ~lines 3471, 3487, 3496 (3 lines)
- check_answer_dict_str_list_str ~lines 3559-3560, 3562, 3574, 3577, 3588, 3592-3594 (8 lines)
- check_answer_list_str ~line 4423 (1 line)
- check_answer_list_tuple_float area ~lines 4464-4467, 4474, 4499, 4501-4502, 4504, 4512 (10 lines)
- check_answer_set_str ~lines 4832, 4835, 4866 (3 lines)
- check_answer_set_tuple_int ~lines 4894-4896, 4902 (4 lines)

IMPORTANT: Line numbers above are approximate from research (file was 5077 lines; after Plan 06-01 changes they will have shifted slightly). Locate each print by searching the function body, not by absolute line number.

After removing all print/pprint calls: also remove the `from pprint import pprint` import near the top of the file (it becomes unused). Verify with grep before removing to confirm no other pprint usage remains.

Run the test suite after this task to catch any regression before proceeding to the next task.
  </action>
  <verify>
    <automated>grep -c "^\s*print(" src/pytest_generator/assert_utilities.py; grep -c "pprint" src/pytest_generator/assert_utilities.py; uv run pytest tests/test_assert_utilities.py -q</automated>
  </verify>
  <done>grep for ^\s*print( returns 0; grep for pprint returns 0; all regression tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Remove commented-out dead code and resolve TODO markers</name>
  <files>src/pytest_generator/assert_utilities.py</files>
  <action>
**Part A — Delete all # ! and # msg_list.append lines (~28 lines).**

Key locations from research (line numbers approximate; search by context):
- ~lines 24, 32: `# ! import matplotlib.pyplot as plt` and `# ! from matplotlib.collections import PathCollection` — delete both
- ~lines 515-516: `# ! return repr(x)...` and `# ! if isinstance(...)` dead alternative logic
- ~lines 543-544: `# msg_list.append(...)` disabled debug appends
- ~lines 1264-1266: `# ! print(...)` block (already commented-out prints)
- ~lines 2055-2059: `# ! i_xlabel = ...` block (3 lines of dead assignments)
- ~line 2669: `# ! choices = ...` dead assignment
- ~lines 3215-3217: `# msg_list.append(f"DEBUG: ...")` cluster (3 lines)
- ~lines 3403-3405: identical DEBUG cluster (3 lines)
- ~line 3675: `# ! TODO: check that keys are in the instructor answer` — handle in Part B
- ~line 3739: `# ! print(f"{i_norms=}...")` already-commented print
- ~lines 3796-3797: `# ! print(...)` and `# ! return False, ""` dead return path
- ~lines 4429-4430: `# msg_list += [...]` pair
- ~line 4981: `# ! print(f"check_answer_str, {remove_spaces=}")`

After deletion: `grep -c "# !" src/pytest_generator/assert_utilities.py` must return 0.

**Part B — Resolve the 3 active TODO markers.**

1. **~line 822**: `## ARE WE CHECKING THAT THE KEYS ARE int ! TODO` inside check_structure_dict_int_dict_str_any.
   - Read the surrounding function body. If the function does verify top-level keys are int, delete the TODO line entirely.
   - If it does NOT verify int keys, replace the `##` double-hash line with a plain single-hash comment: `# Known gap: top-level key type (int) is not explicitly validated.`
   - Either way, remove the `##` double-hash and the word TODO.

2. **~line 3675**: `# ! TODO: check that keys are in the instructor answer` inside check_answer_dict_str_set_int.
   - This line is also a `# !` dead-code line handled in Part A. The TODO embedded in it should be evaluated: if the surrounding function already checks key presence, simply delete the line. If not, replace with a plain comment: `# Known gap: student keys are not verified against instructor_answer keys.`

3. **~line 4427**: `# ! TODO: Explicitly state the indices considered for grading.`
   - Low priority. Convert to a plain comment (remove `# !` and `TODO:`): `# Note: the indices considered for grading are not explicitly stated in the message.`

After all three are resolved, `grep -i "TODO" src/pytest_generator/assert_utilities.py` should return 0 results.
  </action>
  <verify>
    <automated>grep -c "# !" src/pytest_generator/assert_utilities.py; grep -ic "TODO" src/pytest_generator/assert_utilities.py; uv run pytest tests/test_assert_utilities.py -q</automated>
  </verify>
  <done>grep for # ! returns 0; grep for TODO (case-insensitive) returns 0; all regression tests pass.</done>
</task>

</tasks>

<verification>
Full phase gate check after both tasks:
1. `grep -c "^\s*print(" src/pytest_generator/assert_utilities.py` → 0
2. `grep -c "pprint" src/pytest_generator/assert_utilities.py` → 0
3. `grep -c "# !" src/pytest_generator/assert_utilities.py` → 0
4. `grep -ic "TODO" src/pytest_generator/assert_utilities.py` → 0
5. `uv run pytest -q tests/` → all tests pass (>= 183)
</verification>

<success_criteria>
- Zero print() or pprint() statements remain in assert_utilities.py
- Zero # ! or # msg_list.append dead-code comment lines remain
- All three TODO markers are resolved or converted to explanatory plain comments
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/06-detailed-refactoring/06-02-SUMMARY.md` following the summary template.
</output>
