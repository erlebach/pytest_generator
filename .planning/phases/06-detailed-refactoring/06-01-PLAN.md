---
phase: 06-detailed-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pytest_generator/assert_utilities.py
  - tests/test_assert_utilities.py
autonomous: true
requirements:
  - REQ-AUDIT-01

must_haves:
  truths:
    - "check_structure_dict_str_set returns True when the student dict value is a set or list (correct type)"
    - "check_structure_dict_str_set returns False when the student dict value is NOT a set or list (wrong type)"
    - "The stale bare-string block at module scope (lines 35-38) is gone from the file"
    - "All 183 Phase 1 regression tests continue to pass after the bug fix"
  artifacts:
    - path: "src/pytest_generator/assert_utilities.py"
      provides: "logic bug fix on line 1633, stale string block removed"
    - path: "tests/test_assert_utilities.py"
      provides: "corrected regression tests for check_structure_dict_str_set"
  key_links:
    - from: "tests/test_assert_utilities.py"
      to: "src/pytest_generator/assert_utilities.py"
      via: "check_structure_dict_str_set import"
      pattern: "check_structure_dict_str_set"
---

<objective>
Fix the confirmed logic inversion bug in check_structure_dict_str_set, update the regression tests that encoded the buggy behavior, and remove the stale bare-string block at module scope.

Purpose: The bug causes check_structure_dict_str_set to report an error for correctly-typed values and pass for wrong-typed values — the exact opposite of intended behavior. This is a correctness issue that must be fixed before print/comment cleanup to avoid test interference.
Output: assert_utilities.py with corrected logic; tests that verify correct behavior.
</objective>

<execution_context>
@/Users/erlebach/.claude/get-shit-done/workflows/execute-plan.md
@/Users/erlebach/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-detailed-refactoring/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix logic inversion bug and update regression tests</name>
  <files>src/pytest_generator/assert_utilities.py, tests/test_assert_utilities.py</files>
  <action>
In assert_utilities.py, find the condition in check_structure_dict_str_set near line 1633:

    if k in keys and isinstance(v, set | list):

Change it to:

    if k in keys and not isinstance(v, set | list):

Also delete the stale broken comment on the next few lines:

    # msg_list.append(f"- Answer[repr(k)r}] must be of type 'set' or 'list'.")

(The f-string is malformed — `repr(k)r}` is a syntax error in the comment text. Delete the whole line.)

Then update tests/test_assert_utilities.py at lines 491-505 (test_check_structure_dict_str_set_pass and test_check_structure_dict_str_set_fail). These tests currently encode the inverted/buggy behavior as documented by comments like "NOTE: this function has inverted logic". Replace them with tests that verify correct behavior:

- Pass test: student dict value IS a set → expect status True
- Fail test: student dict value is NOT a set/list (e.g., int 42) → expect status False

Remove the "NOTE: this function has inverted logic" and "Function incorrectly fails" comments; replace with a plain description of correct behavior.

Do NOT add new test functions — replace the existing two test functions in-place.
  </action>
  <verify>
    <automated>uv run pytest tests/test_assert_utilities.py -q -k "check_structure_dict_str_set"</automated>
  </verify>
  <done>test_check_structure_dict_str_set_pass passes with a correctly-typed set value input; test_check_structure_dict_str_set_fail passes with a wrong-type input; both tests reflect correct (non-inverted) behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Remove stale module-level bare-string block</name>
  <files>src/pytest_generator/assert_utilities.py</files>
  <action>
Near the top of assert_utilities.py, around lines 35-38, there is a bare string literal (not assigned, not a docstring) that reads:

    """
    # from pprint import pprint
    # import matplotlib.pyplot as plt
    """

Delete all four lines of this block (opening triple-quote, two commented import lines, closing triple-quote). This is dead code masquerading as a comment block. Confirm line numbers first by searching for the string `from pprint import pprint` inside the triple-quoted block context.

After deletion, run the full regression suite to confirm no test breaks (the shift in line numbers is internal only).
  </action>
  <verify>
    <automated>uv run pytest tests/test_assert_utilities.py -q</automated>
  </verify>
  <done>grep for the bare triple-quoted block returns no match; all 183 regression tests pass.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -n 'if k in keys and isinstance' src/pytest_generator/assert_utilities.py` — must return 0 matches for check_structure_dict_str_set (the fixed line now has `not isinstance`)
2. `grep -c '"""' src/pytest_generator/assert_utilities.py | head -5` — stale block gone
3. `uv run pytest tests/test_assert_utilities.py -q` — all tests pass (count >= 183)
</verification>

<success_criteria>
- check_structure_dict_str_set logic is correct: set/list values return True, non-set values return False
- Regression tests reflect correct behavior (no "inverted logic" baseline comments)
- Stale bare-string block at module scope is deleted
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-detailed-refactoring/06-01-SUMMARY.md` following the summary template.
</output>
