---
phase: 03-file-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/pytest_generator/assert_utilities.py
autonomous: true
requirements:
  - REQ-CLEAN-03

must_haves:
  truths:
    - "A SECTION 4 STRUCTURE CHECKS header exists containing all check_structure_X functions"
    - "A SECTION 5 ANSWER CHECKS header exists containing all check_answer_X functions"
    - "check_structure_X and check_answer_X functions are in separate sections (not interleaved)"
    - "Functions within each section are ordered alphabetically by type suffix"
    - "assert_utilities.py imports without error"
    - "All 183 Phase 1 regression tests pass"
  artifacts:
    - path: "src/pytest_generator/assert_utilities.py"
      provides: "Fully reorganized 5-section file"
      contains: "SECTION 4"
  key_links:
    - from: "SECTION 3 end"
      to: "SECTION 4 STRUCTURE CHECKS"
      via: "section header delimiter"
      pattern: "SECTION 4"
    - from: "SECTION 4 end"
      to: "SECTION 5 ANSWER CHECKS"
      via: "section header delimiter"
      pattern: "SECTION 5"
---

<objective>
Reorganize all check_structure_X and check_answer_X functions into two separate, named sections (SECTION 4 and SECTION 5), sorted alphabetically by type suffix within each section.

Purpose: Complete the structural reorganization started in Plan 01. After this plan, assert_utilities.py has a fully navigable 5-section layout with no interleaved checker types.

Output: assert_utilities.py with SECTION 4 (structure checks) and SECTION 5 (answer checks) complete, all tests green.
</objective>

<execution_context>
@/Users/erlebach/.claude/get-shit-done/workflows/execute-plan.md
@/Users/erlebach/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-file-cleanup/03-RESEARCH.md
@.planning/phases/03-file-cleanup/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory and plan the target ordering of checker functions</name>
  <files>src/pytest_generator/assert_utilities.py</files>
  <action>
Before moving any function, build the definitive inventory of all check_structure_X and check_answer_X functions and their current line numbers.

Run:
```bash
cd /Users/erlebach/src/2026/pytest_generator
grep -n "^def check_structure_\|^def check_answer_" src/pytest_generator/assert_utilities.py | sort -t: -k1 -n
```

From the output:
1. Collect all unique type suffixes (the X in check_structure_X and check_answer_X).
2. Sort the suffix list alphabetically.
3. Verify that after Plan 01's moves, no check_structure_X or check_answer_X appears before the end of SECTION 3.

Document the sorted order — this is the target ordering for Tasks 2 and 3. Use this list to drive the moves in Tasks 2-3; do not proceed without it.

Run the import check to confirm baseline health:
```bash
python -c "import src.pytest_generator.assert_utilities; print('baseline OK')"
uv run pytest tests/ -x -q
```
  </action>
  <verify>
    <automated>cd /Users/erlebach/src/2026/pytest_generator && grep -c "^def check_structure_\|^def check_answer_" src/pytest_generator/assert_utilities.py && uv run pytest tests/ -x -q</automated>
  </verify>
  <done>A sorted list of type suffixes exists (either written to terminal or noted). Baseline import and tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Move all check_structure_X functions into SECTION 4</name>
  <files>src/pytest_generator/assert_utilities.py</files>
  <action>
Insert a SECTION 4 header immediately after the last function in SECTION 3:
```python
# ======================================================================
# SECTION 4: STRUCTURE CHECKS
# ======================================================================
```

Move each check_structure_X function body (verbatim, no edits) into SECTION 4 in alphabetical order by type suffix. For example: check_structure_dataframe before check_structure_dict before check_structure_float, etc.

**Execution rules:**
- Move ONE function at a time, or move them all in a single carefully-prepared Edit block — but after each group of moves run the import check.
- Zero logic changes: move blocks exactly as they appear, preserving indentation and blank lines.
- After moving all check_structure_X functions, the zone between SECTION 3 end and SECTION 5 start should contain ONLY check_structure_X functions.

**After all check_structure_X functions are moved:**
```bash
cd /Users/erlebach/src/2026/pytest_generator
python -c "import src.pytest_generator.assert_utilities; print('OK')"
uv run pytest tests/ -x -q
```

If any test fails, stop and diagnose before continuing — a NameError means a callee was moved after a caller.
  </action>
  <verify>
    <automated>cd /Users/erlebach/src/2026/pytest_generator && grep -n "SECTION 4" src/pytest_generator/assert_utilities.py && python -c "import src.pytest_generator.assert_utilities; print('OK')" && uv run pytest tests/ -x -q</automated>
  </verify>
  <done>SECTION 4 STRUCTURE CHECKS header present. All check_structure_X functions appear between the SECTION 4 and SECTION 5 headers. No check_structure_X function appears in the SECTION 5 zone. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Move all check_answer_X functions into SECTION 5</name>
  <files>src/pytest_generator/assert_utilities.py</files>
  <action>
Insert a SECTION 5 header immediately after the last check_structure_X function:
```python
# ======================================================================
# SECTION 5: ANSWER CHECKS
# ======================================================================
```

Move each check_answer_X function body (verbatim, no edits) into SECTION 5 in alphabetical order by type suffix — matching the same suffix order used for check_structure_X in Task 2.

**Execution rules:**
- Same as Task 2: move verbatim, zero logic changes.
- After all check_answer_X functions are moved, the area after SECTION 5 header should be empty (end of file).

**Final verification:**
```bash
cd /Users/erlebach/src/2026/pytest_generator

# Confirm section headers present and in order
grep -n "SECTION [1-5]" src/pytest_generator/assert_utilities.py

# Confirm no check_* functions outside sections 4/5
# (check_structure and check_answer should only appear after their section headers)

# Clean import
python -c "import src.pytest_generator.assert_utilities; print('import OK')"

# Full suite
uv run pytest tests/ -q
```
  </action>
  <verify>
    <automated>cd /Users/erlebach/src/2026/pytest_generator && grep -n "^# ======\|SECTION" src/pytest_generator/assert_utilities.py | grep "SECTION" && python -c "import src.pytest_generator.assert_utilities; print('OK')" && uv run pytest tests/ -q</automated>
  </verify>
  <done>SECTION 5 ANSWER CHECKS header present. All check_answer_X functions appear after the SECTION 5 header. Five section headers exist in ascending line-number order. All 183 regression tests pass. Import succeeds.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/erlebach/src/2026/pytest_generator

# All 5 section headers present and in order
grep -n "SECTION [1-5]" src/pytest_generator/assert_utilities.py

# No check_structure or check_answer functions before SECTION 4
# (line number of first check_structure/answer should be > line number of SECTION 4)
SECTION4_LINE=$(grep -n "SECTION 4" src/pytest_generator/assert_utilities.py | head -1 | cut -d: -f1)
FIRST_CHECKER=$(grep -n "^def check_structure_\|^def check_answer_" src/pytest_generator/assert_utilities.py | head -1 | cut -d: -f1)
echo "SECTION 4 at line $SECTION4_LINE, first checker at line $FIRST_CHECKER"

# Clean import
python -c "import src.pytest_generator.assert_utilities; print('import OK')"

# Full regression suite
uv run pytest tests/ -q
```
</verification>

<success_criteria>
- Five section headers (SECTION 1 through SECTION 5) present in ascending line order
- All check_structure_X functions grouped together in SECTION 4, alphabetically ordered by suffix
- All check_answer_X functions grouped together in SECTION 5, alphabetically ordered by suffix
- No check_structure_X or check_answer_X function appears in Sections 1-3
- `python -c "import src.pytest_generator.assert_utilities"` exits 0
- `uv run pytest tests/ -q` shows all 183 tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/03-file-cleanup/03-02-SUMMARY.md` summarizing:
- Final section layout with line ranges
- Count of check_structure_X and check_answer_X functions moved
- Final file line count
- Any functions that required special handling (call-order issues, etc.)
- Final test count and pass status
</output>
