---
phase: 02-duplicate-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pytest_generator/assert_utilities.py
autonomous: true
requirements:
  - REQ-INFRA-02

must_haves:
  truths:
    - "A grep for duplicate top-level def names in assert_utilities.py returns zero results"
    - "All Phase 1 regression tests pass against the modified assert_utilities.py"
    - "The module imports cleanly (no SyntaxError from incomplete deletion)"
    - "type_handlers.yaml function references all resolve to existing keeper definitions"
  artifacts:
    - path: "src/pytest_generator/assert_utilities.py"
      provides: "Single-definition-per-function source file"
      contains: "no shadowed def blocks"
  key_links:
    - from: "assert_utilities.py"
      to: "type_handlers.yaml"
      via: "function name lookup"
      pattern: "check_(structure|answer)_\\w+"
    - from: "tests/test_assert_utilities.py"
      to: "assert_utilities.py"
      via: "import and call"
      pattern: "from.*assert_utilities import"
---

<objective>
Remove all 7 duplicate function definitions from assert_utilities.py so that every function name is defined exactly once. This is pure dead-code deletion — no logic changes, no renames, no new code.

Purpose: Phase 1 regression tests provide a complete safety net. This phase exploits that safety net to make the file unambiguous about which version of each function is active.
Output: assert_utilities.py with 7 fewer dead definition blocks; all Phase 1 tests still green.
</objective>

<execution_context>
@/Users/erlebach/.claude/get-shit-done/workflows/execute-plan.md
@/Users/erlebach/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-duplicate-removal/02-RESEARCH.md
@.planning/phases/02-duplicate-removal/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Category A duplicate blocks (triple-quote string wrappers)</name>
  <files>src/pytest_generator/assert_utilities.py</files>
  <action>
Remove 5 dead definition blocks that are wrapped in triple-quote string literals. These are dead code disguised as string expressions. For each function below, delete the entire string-wrapped block including its opening and closing delimiter lines. Do NOT touch the keeper definition that follows.

Work through the list in descending line-number order (bottom to top) so that earlier line numbers remain accurate after each deletion.

Removal order (bottom to top):

1. check_answer_dict_str_float — dead block lines 4672–4731 (inside `'''`), keeper at line 4734.
   Read lines 4668–4736 first to confirm boundaries. Delete lines 4672–4731 inclusive (the `'''` open through `'''` close). Keeper's `def` line must remain.

2. check_structure_dict_str_float — dead block lines 4561–4618 (inside `'''`), keeper at line 4620.
   Read lines 4557–4622 first to confirm boundaries. Delete lines 4561–4618 inclusive.

3. check_answer_randomforestclassifier — dead block lines 4469–4509 (inside `'''`), keeper at line 4512.
   Read lines 4465–4514 first to confirm boundaries. Delete lines 4469–4509 inclusive.

4. check_structure_shufflesplit — dead block lines 4335–4351 (inside `"""`), keeper at line 4354.
   Read lines 4331–4356 first to confirm boundaries. Delete lines 4335–4351 inclusive.

5. check_structure_kfold — dead block lines 4206–4222 (inside `"""`), keeper at line 4225.
   Read lines 4202–4227 first to confirm boundaries. Delete lines 4206–4222 inclusive.

After each deletion, run:
  cd /Users/erlebach/src/2026/pytest_generator && python -m pytest tests/test_assert_utilities.py -x -q

All 5 deletions must be completed with tests passing before committing. If any test fails, restore the file from git (git checkout src/pytest_generator/assert_utilities.py) and diagnose the boundary error before retrying.

Pitfalls to avoid:
- Do not delete the keeper's `def` line — it must survive as the first line after each gap.
- Remove the opening `"""` / `'''` line, all body lines, and the closing `"""` / `'''` line as one contiguous block.
- Work bottom-to-top so line numbers for earlier blocks are not shifted by previous deletions.
  </action>
  <verify>
    <automated>cd /Users/erlebach/src/2026/pytest_generator && python -m pytest tests/test_assert_utilities.py -x -q && echo "TESTS PASS" && grep -n "^def check_structure_kfold\|^def check_structure_shufflesplit\|^def check_answer_randomforestclassifier\|^def check_structure_dict_str_float\|^def check_answer_dict_str_float" src/pytest_generator/assert_utilities.py</automated>
  </verify>
  <done>Each of the 5 function names appears exactly once in grep output, and the test suite is fully green.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Category B duplicate blocks (live shadow definitions)</name>
  <files>src/pytest_generator/assert_utilities.py</files>
  <action>
Remove 2 dead definition blocks where two syntactically-live `def` statements exist for the same name. Python uses the later one; the earlier one is dead at runtime. Delete only the earlier (dead) block.

IMPORTANT: Before deleting either block, read both blocks side-by-side to confirm the keeper is unambiguously the later one and that deletion is safe.

Work in descending line-number order (bottom to top):

1. check_answer_dict_str_int
   - Dead block: approximately lines 1796–1884 (earlier definition)
   - Keeper: approximately lines 2348–2436 (later definition, Python-active)
   - Steps:
     a. Run: grep -n "^def check_answer_dict_str_int" src/pytest_generator/assert_utilities.py
        Confirm exactly two hits; note the lower line number (dead) and higher line number (keeper).
     b. Read lines (dead_start - 2) through (dead_end + 3) to confirm block boundaries.
        The dead block ends where the next top-level `def` or section comment begins.
     c. Delete the dead block (earlier definition) from its `def` line through the last line of its body.
     d. Run: cd /Users/erlebach/src/2026/pytest_generator && python -m pytest tests/test_assert_utilities.py -x -q
        All tests must pass before continuing.

2. check_structure_dict_str_list_str
   - Dead block: approximately lines 826–1007 (earlier definition, uses clean_str_answer() key normalization)
   - Keeper: approximately lines 1009–~1100 (later definition, set-based matching, Python-active)
   - Note: The earlier definition uses a different algorithm (clean_str_answer() key normalization). This is safe to remove because Phase 1 tests were written against the runtime behavior (the later definition). Removing the earlier definition changes nothing at runtime.
   - Steps:
     a. Run: grep -n "^def check_structure_dict_str_list_str" src/pytest_generator/assert_utilities.py
        Confirm exactly two hits.
     b. Read lines (dead_start - 2) through (dead_end + 3) to confirm block boundaries.
        The dead block likely ends around line 1007 where a `# ---` separator precedes the keeper at line 1009.
     c. Delete the dead block from its `def` line through the last line of its body (including any trailing blank lines that are part of the block, but not the `# ---` separator that introduces the keeper).
     d. Run: cd /Users/erlebach/src/2026/pytest_generator && python -m pytest tests/test_assert_utilities.py -x -q
        All tests must pass.

If any test fails after either deletion, immediately restore:
  git checkout src/pytest_generator/assert_utilities.py
Then re-read the boundary lines and correct the deletion range.
  </action>
  <verify>
    <automated>cd /Users/erlebach/src/2026/pytest_generator && python -m pytest tests/test_assert_utilities.py -x -q && echo "TESTS PASS" && grep -n "^def check_answer_dict_str_int\|^def check_structure_dict_str_list_str" src/pytest_generator/assert_utilities.py</automated>
  </verify>
  <done>Each of the 2 function names appears exactly once in grep output, and all regression tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Final audit — confirm zero duplicates and clean import</name>
  <files>src/pytest_generator/assert_utilities.py</files>
  <action>
Run the full duplicate audit to confirm zero duplicate top-level `def` names remain in assert_utilities.py. Also verify the module imports cleanly and the full test suite (not just assert_utilities tests) passes.

Run each of these in sequence:

1. Duplicate name check (expect empty output):
   grep -n "^def " src/pytest_generator/assert_utilities.py \
     | awk -F'[(]' '{print $1}' \
     | sort | uniq -d

2. Clean import check:
   cd /Users/erlebach/src/2026/pytest_generator && python -c "import src.pytest_generator.assert_utilities; print('import OK')"

3. Full regression suite:
   cd /Users/erlebach/src/2026/pytest_generator && python -m pytest tests/test_assert_utilities.py -q

4. Full project test suite:
   cd /Users/erlebach/src/2026/pytest_generator && python -m pytest tests/ -q

If the duplicate check returns any output, the deletion in Task 1 or 2 was incomplete. Identify which function still has two definitions, re-read the file around that function, and remove the earlier block.
  </action>
  <verify>
    <automated>cd /Users/erlebach/src/2026/pytest_generator && grep -n "^def " src/pytest_generator/assert_utilities.py | awk -F'[(]' '{print $1}' | sort | uniq -d | wc -l | xargs -I{} test {} -eq 0 && echo "ZERO DUPLICATES" && python -m pytest tests/ -q</automated>
  </verify>
  <done>Duplicate grep returns zero lines; module imports without error; all tests in tests/ pass.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Zero duplicates: `grep -n "^def " src/pytest_generator/assert_utilities.py | awk -F'[(]' '{print $1}' | sort | uniq -d` returns empty.
2. All 182 Phase 1 tests pass: `python -m pytest tests/test_assert_utilities.py -q`
3. Module imports cleanly: `python -c "import src.pytest_generator.assert_utilities"`
4. Full suite green: `python -m pytest tests/ -q`
</verification>

<success_criteria>
- assert_utilities.py contains exactly one `def` block per function name (grep confirms zero duplicates)
- All Phase 1 regression tests pass (no regressions introduced)
- The module imports without SyntaxError or NameError
- type_handlers.yaml continues to reference only names that exist in the keeper definitions (no YAML edits needed)
</success_criteria>

<output>
After completion, create `.planning/phases/02-duplicate-removal/02-01-SUMMARY.md` following the summary template.
</output>
