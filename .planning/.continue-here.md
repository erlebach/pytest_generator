---
phase: new-project-initialization
task: 4
total_tasks: 6
status: in_progress
last_updated: 2026-03-10T20:48:49.892Z
---

<current_state>
Mid /gsd:new-project workflow. PROJECT.md and config.json are committed. Currently at the
requirements definition step — requirements have been discussed and agreed upon verbally
but REQUIREMENTS.md has not been written yet. Roadmap has not been created yet.
</current_state>

<completed_work>

- Step 1: GSD init check — confirmed project_exists=false, is_brownfield=true, has_codebase_map=true
- Step 2: PROJECT.md written and committed (cff9404)
- Step 3: config.json written and committed (8b7a09a)
  - mode: yolo, granularity: standard, parallelization: true
  - workflow: research=true, plan_check=true, verifier=true
  - model_profile: balanced
- Step 4: Requirements discussed and agreed upon (verbally, not yet written)

</completed_work>

<remaining_work>

- Write REQUIREMENTS.md with the agreed requirements below
- Skip research (domain is well-understood — this is a refactoring of an existing codebase)
- Spawn roadmapper to create ROADMAP.md and STATE.md
- Present roadmap for approval

</remaining_work>

<decisions_made>

- Refactor assert_utilities.py only (test_generator.py is frozen for now; future phase)
- Keep single file (no splitting by type — composite types make clean splits non-obvious)
- Functional only — no classes
- tuple[bool,str] return format maintained for all public (check_structure_X / check_answer_X) functions
- Internal helpers may have mixed return types — that's fine, clean up freely
- External interface of test_generator.py must not break
- Internal boundary (function names referenced in type_handlers.yaml) may change
- New types will be added in the future; auto-generation of checker code is a longer-term possibility

</decisions_made>

<blockers>
None.
</blockers>

<context>
The codebase is assert_utilities.py (5660 lines, ~100 functions) which validates student
answers against instructor answers for a pytest-based autograder. The file has:
- Duplicate function definitions (e.g., check_structure_dict_str_list_str at lines 826 and 1009,
  check_structure_kfold at 4207 and 4225, check_answer_dict_str_float at 4673 and 4734,
  check_structure_dict_str_float at 4562 and 4620, check_answer_randomforestclassifier at
  4470 and 4512, check_structure_shufflesplit at 4336 and 4354)
- Mixed internal return types (tuple[bool,str] vs tuple[bool,list[str]])
- Repeated boilerplate per type (every type needs check_structure_X + check_answer_X)
- No clear template for adding new types

The agreed requirements are:

INFRA:
- REQ-INFRA-01: Comprehensive regression tests for all existing checker functions (BEFORE refactoring)
- REQ-INFRA-02: Identify and resolve all duplicate function definitions

CLEANUP:
- REQ-CLEAN-01: Extract shared primitives into clearly delineated utilities section
- REQ-CLEAN-02: Add type aliases (PartialScoreDict, CheckResult, etc.)
- REQ-CLEAN-03: Reorganize file into sections: utilities → primitives → structure checks → answer checks

CONSISTENCY:
- REQ-CONS-01: Standardize check_structure_X / check_answer_X signature conventions
- REQ-CONS-02: Audit and confirm all public functions return tuple[bool, str]; fix any that don't

EXTENSIBILITY:
- REQ-EXT-01: Document template/recipe for adding a new type
- REQ-EXT-02: Apply template retroactively to one existing type as reference implementation

</context>

<next_action>
Resume with: /gsd:resume-work
The workflow should pick up at writing REQUIREMENTS.md, then spawn roadmapper.
Or manually: write REQUIREMENTS.md from the requirements listed above, then run roadmapper.
</next_action>
